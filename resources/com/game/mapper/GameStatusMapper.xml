<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.game.mapper.BattleMapper">

    <update id="updateBattleCards" parameterType="com.game.dto.Battle">
        UPDATE battles
        SET our_card1=#{ourCard1},
        our_card2=#{ourCard2},
        our_card3=#{ourCard3},
        enemy_card1=#{enemyCard1},
        enemy_card2=#{enemyCard2},
        enemy_card3=#{enemyCard3}
        WHERE room_id=#{roomId}
    </update>
    <update id="updateBattleGameOver">
        UPDATE battles
        SET winner = #{playerId1},
        loser = #{playerId2},
        status = 0
        WHERE room_id=#{roomId}
    </update>
    <update id="updateBattleWithDraw">
        UPDATE battles
        set status = 2
        WHERE room_id=#{roomId}
    </update>
    <update id="updateBattleTurn">
        UPDATE battles
        set turn = #{turn}
        WHERE room_id=#{roomId}
    </update>
    <update id="updateBattleExcept">
        UPDATE battles
        set status = 3
        WHERE room_id=#{roomId}
    </update>
</mapper>